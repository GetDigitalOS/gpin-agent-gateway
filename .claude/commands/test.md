# Generate Tests

Create comprehensive tests for: $ARGUMENTS

Follow the project's testing conventions:
1. Check existing test files for patterns and test framework usage
2. Place tests adjacent to source files or in the project's test directory (match existing convention)
3. Test the happy path, edge cases, error cases, and boundary conditions
4. Mock external dependencies appropriately
5. Ensure tests are isolated — no shared state, deterministic, order-independent
6. Run the tests after writing them to confirm they pass
